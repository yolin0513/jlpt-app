# JLPT 練習 — 日文檢定 N1–N5＋生活旅行

**線上版：https://yolin0513.github.io/jlpt-app/**（手機開啟後可「加入主畫面」安裝，安裝後可離線使用）

手機優先的 **PWA**（漸進式網頁應用程式），用來練習 JLPT N1～N5 的**單字**與**文法**，
另附一組與級別無關的**生活旅行用語**（情境會話、日本人實際用法、中日漢字差異）。
可加到手機主畫面、支援離線使用，所有學習資料只存在你的瀏覽器裡，不需要註冊、不需要後端伺服器。

介面語言為**繁體中文**，日文內容皆標註**假名讀音**。

---

## 功能

| 功能 | 說明 |
| --- | --- |
| 🃏 閃卡背誦 | 正面日文（漢字）、翻卡看假名／中文釋義／例句，並自我評分（不會／模糊／認得） |
| 📝 四選一測驗 | 日→中、中→日、文法↔意思等多種出題方向，作答後即時顯示解說與例句 |
| 🈯 漢字讀音 | JLPT 文字語彙第一大題的題型。題目綁在「詞」上而非單一漢字；干擾選項依拍數／首尾音挑「音近」的讀音，並排除同形異讀與同音異字以免出現兩個正解 |
| ✏️ 例句填空 | JLPT 文脈規定題型。挖掉例句裡的目標單字或文法（不是隨機挖）；干擾選項以「空格前後的助詞是否吻合」為主要依據，確保四個選項都塞得進句子。題目不顯示中譯，作答後才揭曉 |
| 🏷️ 詞性篩選 | 學習頁可以只練某一種詞性（動詞／い形容詞／な形容詞／名詞／副詞／其他），每個選項旁邊是該級實際有幾個字；測驗與閃卡都吃這個篩選，可與「漢字讀音」「例句填空」同時成立。干擾選項也優先挑同詞性的，免得看詞性就能刪掉選項 |
| 🔁 間隔重複（SRS） | Leitner box 制：答對逐步拉長複習間隔（10 分鐘 → 1 → 2 → 4 → 8 → 16 → 32 天），答錯歸零並排入近期複習 |
| ❗ 錯題本 | 測驗／閃卡答錯的項目自動收錄，可依級別／類型篩選、單獨練習、掌握後自動標記訂正 |
| 🧳 生活旅行 | 與 JLPT 平行的獨立分類，見下方專節。同樣支援閃卡、測驗、SRS、錯題本，統計分開顯示 |
| ★ 重點複習 | 在任何地方點 ☆ 收藏單字／文法／旅行用語，集中以閃卡或測驗複習 |
| 🔍 搜尋 | 跨全部級別＋生活旅行即時搜尋漢字、假名、羅馬字或中文釋義；結果可直接收藏、朗讀 |
| 🎧 聽力練習 | 聽日文句子選出中文意思。音源用裝置內建語音（零成本、免金鑰、本機語音可離線）；**偵測不到日文語音時不會給死按鈕**，改顯示各平台安裝教學與「重新偵測」。播放時長會被檢查，異常時主動提示並提供「顯示日文原文」的退路 |
| 🔊 日文朗讀 | 以 Web Speech API 朗讀日文（單字例句、文法例句），可設定翻卡時自動朗讀。完全在裝置端運作 |
| 📊 學習統計 | 學習熱力圖（深淺對照每日目標，範圍 12～53 週）、各級別掌握度（已掌握／學習中）、總正確率、連續學習天數 |
| ⏱️ 模擬考 | 分科計時（文字・語彙／文法／聽解），作答中不給對錯、可跳題改答案，全科結束才結算；時間照真實 JLPT 節奏，合格判定照官方基準換算。**不含讀解**（沒有讀解題庫，畫面上會講明） |
| 📅 複習預測 | 複習頁畫出未來 14 天每天會到期幾項，看得出哪天會塞車 |
| 🩹 弱點清單 | 依錯誤率、是否還沒掌握、累計錯誤次數排出「最學不起來」的項目，可直接針對它們練一輪 |
| 🎯 每日目標 | 可自訂每日題數目標（預設 20），首頁顯示達成進度 |
| ⌨️ 鍵盤快捷鍵 | 測驗：`1`–`4` 選項、`Enter`/`空白` 下一題、`S` 朗讀；閃卡：`空白` 翻卡、`1`/`2`/`3` 評分、`S` 朗讀、`Esc` 結束 |
| 🌓 深色 / 淺色 | 手動切換，預設跟隨系統 |
| 💾 資料匯出 / 匯入 | 以 JSON 備份或轉移學習進度（含最愛，備份格式 v3）；**匯入會取代目前的資料**（會先跳確認框）；可一鍵重置 |
| 🔔 備份提醒 | 距上次匯出超過 14 天、而且這段期間真的有練，首頁才出現一張提醒卡，可一鍵匯出或「這週先不要」（延後 7 天，沒有永久關閉）。按匯出時順便向瀏覽器請求持久儲存，統計頁照實顯示瀏覽器答應了什麼 |
| 👋 首次引導 | 初次開啟時顯示簡短使用說明 |

### 出題範圍選項
- **智慧排程**：已到期的複習題優先（最多佔一輪七成），其餘補未學過的新題，再其他。
  複習優先才是間隔重複的重點——新題優先會讓到期題永遠排不進來，掌握度就永遠不動。
- **隨機**
- **依序**：照題庫順序

出題來源除了「指定級別題庫」外，也可從**待複習項目**、**錯題本**、**重點複習**、**弱點清單**、**生活旅行分類**組卷。

---

## 生活旅行用語（與 JLPT 平行的分類）

首頁「生活旅行用語」入口，或 `#/travel`。內容與 JLPT 級別完全獨立（進度、統計分開計算），
但共用閃卡、四選一測驗、間隔重複與錯題本。分成三塊：

| 分類 | 內容 | 練習時 |
| --- | --- | --- |
| 💬 情境會話 | 機場入境／領行李、電車／巴士／計程車、住宿 check-in/out 與客訴、餐飲進店／點餐／用餐／結帳、購物／退稅、藥妝店、便利商店、問路、生病、遺失物品與報警等自由行實用句 | 正面看中文情境 → 翻卡／選出對應日文（含假名）。可再依「情境」細分練習 |
| 🗣️ 日本人這樣說 | 店員的固定問句（加熱？袋子？集點卡？內用外帶？）與你該怎麼回、居酒屋「お通し」、電車站內廣播、以及「課本 vs 現實」的委婉說法（お手洗い／〜ていただけますか／けっこうです…） | 同上，備註欄提示「你可以這樣回應」 |
| 🀄 中日漢字大不同 | 同一漢字在中日文語義落差大的詞，逐條列「日文讀音／日文意思／台灣人常誤解成什麼／例句」。例：**床**（日文＝地板，不是睡覺的床）、手紙（信）、勉強（唸書）、汽車（蒸汽火車）、丈夫（結實）、大家（房東）、切手（郵票）、人参（紅蘿蔔）、迷惑（困擾）、階段（樓梯）、無理、心中（殉情）、新聞（報紙）、精進、油断、愛人（情婦）、放心（發呆）、留守（不在家）… | 正面看漢字 → 翻卡／選出日文意思；或反向由意思選漢字 |

目前份量：情境會話 142 條、日本人這樣說 54 條、中日漢字大不同 70 條，共 266 條。

### 增補生活旅行內容

| 檔案 | 每行格式 |
| --- | --- |
| `data/src/travel.phrases.txt` | `場景 \| 日文 \| 假名 \| 中文 \| 備註(可空)` |
| `data/src/travel.usage.txt` | `場景 \| 日文 \| 假名 \| 中文 \| 你可以這樣說／說明(可空)` |
| `data/src/travel.kanji.txt` | `漢字 \| 日文讀音 \| 日文意思 \| 台灣人常誤解 \| 例句 \| 例句假名 \| 例句中譯` |

編輯後同樣執行 `python scripts/build_data.py`（產生 `data/travel/*.json` 並更新 `manifest.json` 的 `travel` 區塊），
再跑 `python scripts/check_data.py` 檢查。id 為 `tv-p-0001`／`tv-u-0001`／`tv-k-0001`，新增請加在檔案末端。

---

## 技術棧

- 純 **原生 JavaScript（ES Modules）**，無框架、無打包工具
- **IndexedDB** 儲存學習進度、錯題、每日統計、最愛（DB 版本 2，升級只新增 store、不動舊資料）
- **Web Speech API** 提供裝置端日文朗讀（無網路需求）
- **Service Worker + Web App Manifest** 提供離線與「加入主畫面」能力
- 題庫為可擴充的 **JSON** 檔（由 `data/src/*.txt` 產生）
- 開發用靜態伺服器以 **Python 標準函式庫** 實作（`scripts/serve.py`）
- 題庫品質檢查腳本 `scripts/check_data.py`（欄位、假名、重複、抽樣）

> 之所以選這個組合：打開檔案就能改、學習曲線低、日後只要編輯純文字題庫再跑一行指令即可增補內容。

---

## 部署（GitHub Pages）

已部署於 `https://yolin0513.github.io/jlpt-app/`（`main` 分支根目錄，含 `.nojekyll`）。
專案內所有路徑都是相對路徑（`./`、`../` 搭配 `import.meta.url`），子路徑部署不需任何調整。
推新版到 `main` 後，GitHub Pages 會自動重建；使用者端 Service Worker 會在下次連線時抓到新版
（`sw.js` 的 `VERSION` 有變更時會清掉舊快取）。

驗證線上版：`node scripts/verify-live.mjs`（檢查 SW 註冊、題庫載入、離線可用，並截一張線上首頁圖）。

## 本機啟動

Service Worker 需要透過 `http(s)://`（或 `localhost`）載入，**不能用 `file://` 直接開啟**。任選一種方式起一個本機伺服器：

### 方式 A：專案內建伺服器（建議）

```bash
cd D:\Claude\App\JLPT_App
python scripts/serve.py            # 預設 http://localhost:5173
python scripts/serve.py 8000      # 或自訂埠號
```

內建伺服器為多執行緒，並設定正確的 MIME 類型與「不快取」標頭，適合開發。

### 方式 B：Python 內建模組（不建議在 Windows 用）

```bash
cd D:\Claude\App\JLPT_App
python -m http.server 5173
```

> ⚠️ 在 Windows 上，單執行緒的 `http.server` 遇到瀏覽器同時抓多個題庫檔時，曾出現 `net::ERR_NO_BUFFER_SPACE`，
> 這正是寫 `scripts/serve.py` 的原因；它也沒有「不快取」標頭，測 Service Worker 更新時容易拿到舊檔。請優先用方式 A。

### 方式 C：Node

```bash
cd D:\Claude\App\JLPT_App
npx serve -l 5173
# 或： npx http-server -p 5173
```

開啟瀏覽器到 `http://localhost:5173/`。
用手機測試時，讓手機與電腦連同一個 Wi-Fi，開 `http://<電腦區域網路IP>:5173/`。

### 安裝到手機主畫面
- **iOS Safari**：分享 → 加入主畫面
- **Android Chrome**：選單 → 安裝應用程式 / 加入主畫面

安裝後首次連線會把 App 與題庫快取起來，之後即可離線使用。

### 產生畫面截圖（開發用）

```bash
npm install                       # 安裝 puppeteer（會下載一份 Chromium）
python scripts/serve.py 5173      # 另開一個視窗跑伺服器
node scripts/screenshots.mjs      # 產生 screenshots/*.png（手機尺寸 390x844）
```

腳本會先在瀏覽器裡塞一批示範學習紀錄，再依序截取首頁、學習、生活旅行、閃卡、
中日漢字閃卡、測驗、測驗結果、旅行測驗、錯題本、重點複習、複習、統計、搜尋、深色首頁
（另有 360px 與深色兩組變體，已 gitignore）。

> `screenshots/` 裡的圖停在 **v1.4.0**，沒有聽力、模擬考、弱點清單、熱力圖等新畫面，腳本清單也還沒涵蓋它們。
> 目前的開發規則是「除非 Yolin 要求，否則不主動截圖」，所以暫不重拍；需要時先擴充腳本清單再重拍。

---

## 專案結構

```
JLPT_App/
├── CLAUDE.md               給 Claude 的專案指示（新 Session 自動載入；開工前先讀 docs/STATUS.md）
├── index.html              進入點（App 外殼）
├── manifest.webmanifest    PWA 設定
├── sw.js                   Service Worker（殼層 stale-while-revalidate、題庫 cache-first、manifest network-first）
├── css/style.css
├── docs/
│   ├── STATUS.md           專案狀態、交接、流程慣例、決策紀錄、踩過的坑（接手必讀）
│   └── SPEC_*.md           規劃中功能的規格草稿
├── js/
│   ├── app.js              啟動、路由註冊（動態 import 延遲載入）、主題、SW 註冊
│   ├── router.js           hash 路由（safeDecode 防畸形網址）
│   ├── db.js               IndexedDB 封裝（jlpt_app v2：progress / mistakes / daily / meta / favorites）
│   ├── store.js            進度／錯題／統計／最愛／設定、匯出匯入（備份格式 v3）
│   ├── srs.js              間隔重複排程、學習階段 stageOf()、到期預測 forecast()
│   ├── data.js             題庫載入、去重過濾、搜尋索引、並發控制
│   ├── session.js          組出一輪題目（級別／複習／錯題／最愛／弱點／旅行）與 smart 排程
│   ├── qtypes.js           特殊題型：漢字讀音、例句填空、聽力素材 audioOf()
│   ├── weak.js             弱點排序 weakScore()
│   ├── speech.js           日文朗讀（Web Speech API）、語音偵測、speakChecked()
│   ├── keys.js             鍵盤快捷鍵綁定（畫面切換自動解除）
│   ├── itemview.js         單字／文法／旅行的共用元件（★ 最愛、🔊 朗讀、詳細卡片）
│   ├── ui.js               DOM 小工具、進度條（含「學習中」淺色段）
│   └── views/              各畫面：home / learn / travel / flashcards / quiz / review / mistakes /
│                           stats / search / favorites / listening / exam / weak
├── data/
│   ├── manifest.json       題庫索引（自動產生；含 dataVersion、activeItems、dupIds、travel 區塊）
│   ├── search-index.json   搜尋用精簡索引（自動產生，不含例句）
│   ├── vocab/{n5..n1}.json 單字題庫（自動產生）
│   ├── grammar/{n5..n1}.json 文法題庫（自動產生）
│   ├── travel/{phrases,usage,kanji}.json 生活旅行（自動產生）
│   └── src/                ← 你要編輯的地方（純文字；新條目只能加在檔尾）
│       ├── vocab.n5.txt ... vocab.n1.txt
│       ├── grammar.n5.txt ... grammar.n1.txt
│       ├── travel.phrases.txt / travel.usage.txt / travel.kanji.txt
│       └── dedup.txt       跨級別重複清單（載入時隱藏較高級別那筆，不刪行）
├── icons/                  PWA 圖示
├── screenshots/            手機尺寸截圖（由 scripts/screenshots.mjs 產生）
└── scripts/
    ├── build_data.py       由 data/src/*.txt 產生 data/*.json、manifest、搜尋索引、羅馬字
    ├── check_data.py       題庫品質檢查（--sample N 可隨機抽樣）
    ├── audit.mjs           全面回歸測試（148 項）
    ├── verify-full.mjs     完整驗證：各分頁、深色、RWD、SW 更新、離線（30 項）
    ├── regress.mjs         路由與主要互動回歸（23 項）
    ├── verify-live.mjs     線上部署驗證＋線上首頁截圖
    ├── screenshots.mjs     用 puppeteer 產生各畫面截圖
    ├── make_icons.py       產生 PWA 圖示（需 Pillow）
    └── serve.py            開發用伺服器（多執行緒，Windows 必用）
```

> `node_modules/`（puppeteer）只給測試與截圖腳本用，執行 App 本身完全不需要（純靜態）。

---

## 如何增補題庫

題庫內容一律寫在 `data/src/` 的純文字檔，欄位以 `|`（或全形 `｜`）分隔，`#` 開頭為註解。

### 1. 單字：`data/src/vocab.<級別>.txt`

```
漢字 | 假名 | 中文釋義 | 詞性 | 例句(可空) | 例句假名(可空) | 例句中譯(可空)
```

範例：

```
勉強 | べんきょう | 學習、唸書 | 名詞 | 毎日日本語を勉強します。 | まいにちにほんごをべんきょうします。 | 每天學日語。
早い | はやい | 早的、快的 | 形容詞（い形） | 朝が早いです。 | あさがはやいです。 | 早上起得很早。
```

- 只有前三欄（漢字、假名、中文釋義）是必填，其餘可留白（`漢字 | かな | 意思 | | | |`）。
- 沒有漢字的詞（外來語等），漢字欄直接寫假名即可。

### 2. 文法：`data/src/grammar.<級別>.txt`

```
句型 | 讀音(可空) | 中文意思 | 接續結構 | 例句 | 例句假名 | 例句中譯
```

範例：

```
～たことがあります | | 「曾經～過」表示經驗 | 動詞た形＋ことがあります | 富士山に登ったことがあります。 | ふじさんにのぼったことがあります。 | 曾經爬過富士山。
```

### 3. 重新產生 JSON 並檢查

```bash
python scripts/build_data.py     # 產生 data/*.json 與 manifest.json
python scripts/check_data.py     # 檢查欄位缺漏、假名格式、重複條目、數量一致
python scripts/check_data.py --sample 30   # 另外隨機抽 30 條供人工核對
```

`build_data.py` 會依照各檔案目前的順序重新編號 id（`n5-v-0001`、`n3-g-0012`…），
並更新 `data/manifest.json` 的各級別數量。

`check_data.py` 會驗證：必填欄位（漢字／句型、中文釋義、詞性）不為空、假名欄只含假名、
有例句就要有例句假名、同級別內無重複條目（單字依 漢字＋假名、文法依句型）、
各 JSON 的 `count` 與 `manifest.json` 一致、全域 id 唯一。有問題會以非零結束碼回報。

> ⚠️ id 是依行序產生的。如果你在檔案**中間插入**新詞，後面所有詞的 id 會位移，
> 已學習者的進度紀錄會對不上那些位移的項目。建議**新增時往檔案末端加**（本專案增補的批次都放在各檔末尾的「=== 增補批次 N ===」註解之後）。

### 4. 不要直接編輯 JSON

`data/**/*.json`、`data/manifest.json`、`data/search-index.json` 一律由 `build_data.py` 產生，**不要手改**：
手改的內容下次 build 會被整個覆蓋；`dataVersion` 也不會變，使用者手機上的 Service Worker 題庫快取永遠不會失效；
`activeCount`、搜尋索引、羅馬字也不會跟著更新。要改題庫，一律改 `data/src/*.txt` 再重新 build。

### 新增一個全新級別檔的注意事項
`build_data.py` 只認得 `N5`–`N1` 與三個生活旅行分類。若某級別原本沒有檔案，新增 `data/src/vocab.n2.txt` 後再跑腳本即可，
`manifest.json` 會自動補上該組。`sw.js` 的 `DATA_ASSETS` 已列出全部 10 個級別檔、3 個生活旅行檔與 `search-index.json`，頁面載入後由 `WARM_DATA` 在背景快取；題庫內容一改，`dataVersion` 就會變，SW 會自動清掉舊快取重抓，不必手動升版號。

---

## 目前題庫份量

（v1.13.0，數字取自 `data/manifest.json`）

| 級別 | 單字 | 文法 |
| --- | --- | --- |
| N5 | 646 | 116 |
| N4 | 501 | 120 |
| N3 | 508 | 125 |
| N2 | 502 | 127 |
| N1 | 514 | 120 |
| **JLPT 合計** | **2671** | **608** |

| 生活旅行 | 條數 |
| --- | --- |
| 情境會話 | 142 |
| 日本人這樣說 | 54 |
| 中日漢字大不同 | 70 |
| **生活旅行合計** | **266** |

**全部合計 3545 條**：JLPT 3279 條（其中 28 筆為跨級別重複，已隱藏於出題，實際可練 3251 條）＋生活旅行 266 條。
五級單字皆 500 條以上、文法皆 116 點以上（依歷屆出題頻率選收）。
（`check_data.py` 檢查讀音純假名、空欄位、跨級別重複收錄；新增條目一律加在 `data/src/*.txt` 末端以免既有 id 位移。
同一個詞若已在較低級別收錄，把較高級別那筆列進 `data/src/dedup.txt`，build 時會標成 `dup` 並排除於出題／搜尋，
但 id 不動、`findItem()` 仍解析得到 → 既有學習進度不受影響。）

---

## 版本紀錄

- **v1.15.0** — 詞性篩選（第一步）。學習頁在選單字時多一排詞性，選項是**全部／動詞／い形容詞／な形容詞／名詞／副詞／其他**，每個選項旁顯示該級實際筆數（從題庫算，不寫死；0 個的不顯示）。選了之後，從學習頁開始的**測驗與閃卡都只出那個詞性**，而且與既有的題型篩選（漢字讀音、例句填空）是「兩個條件都要成立」而不是互相覆蓋。只作用在一般題庫：複習、錯題本、收藏、弱點、聽力、模擬考、旅行都不受影響。四選一的干擾選項改成**優先挑同詞性**，同詞性不夠才往外補——不然選了動詞還混進名詞，看詞性就能刪掉選項。選擇會被記住（`meta.lastPos`），該詞性在新級別是 0 個時自動退回「全部」。SRS 記分完全不變。
  **注意這只是第一步**：題庫目前沒有任何活用（て形、ない形…）資料，動詞也還沒分一類／二類／三類，所以這一版**沒有**為「詞性變化」預留任何程式或欄位。

- **v1.14.0** — 備份提醒與持久儲存。學習進度只存在使用者自己的瀏覽器、沒有任何後端副本，而 App 從來不提醒備份。現在：距上次匯出超過 14 天**而且**這段期間有新的作答紀錄時，首頁出現一張提醒卡（一鍵匯出／「這週先不要」延後 7 天；沒有永久關閉）；從沒匯出過的人要先學滿 3 天才提醒，剛裝好的不打擾。匯出成功才記 `lastExportAt`，失敗不記；匯入備份後 `lastExportAt` 改成那份備份的產生時間。另外在使用者按下匯出時（要有使用者手勢）呼叫 `navigator.storage.persist()`，統計頁用一行字照實寫瀏覽器答應了什麼——不講成「資料安全了」。不動 IndexedDB schema（只多兩個 `meta` key）、不動備份格式 v3、不動 SW 快取策略。

- **v1.13.0** — 修掉使用者實測回報的兩個問題。
  **(1) 首頁「N5 快速測驗」會出 N1~N5 的題**。根因不在按鈕——按鈕帶的 URL
  是對的（`level=N5`），問題在 `buildSession` 的
  `if (o.level === 'ALL' || o.src === 'mix')`：**只要 `src='mix'` 就無條件載入全部五級**，
  把明確指定的級別蓋掉。`src='mix'` 全專案只有首頁這顆鈕在用，呼叫端的「mix」
  是指混合新題與到期題，被呼叫端卻實作成「混合級別」，兩邊對同一個字的理解不一樣。
  已移除該分支，級別一律以 `o.level` 為準，要跨級請用 `level='ALL'`。
  其餘帶級別的入口（各級測驗／閃卡／聽力／漢字讀音／例句填空／模擬考）逐一驗過，沒有同樣問題。
  **(2) 做了很多測驗但掌握度不動**——是設計 bug，不是誤解。實測：
  **連做 200 題全對，已掌握 0、待複習也 0**。三個原因疊在一起：
  ① `orderPool` 的 smart 是「未學過 → 已到期 → 其他」，題庫有三千多條，
  未學過的永遠填滿整輪 20 題，**到期的複習題永遠排不進來** →
  沒有任何項目升得到 box 3，掌握度結構性地卡在 0。
  ② 「已掌握」要答對 3 次、間隔 1 天→2 天，最快跨 3 天，但畫面沒有任何地方說明，
  也看不到中間狀態，使用者只看到一個不動的 0。
  ③ 第一天練完所有 due 都在隔天，首頁只寫「目前沒有待複習項目」，
  看起來像練了等於沒練。
  修正：smart 改成**到期題優先（最多佔一輪七成，`DUE_QUOTA`），其餘補新題**；
  新增 `stageOf()`／`tallyStages()` 區分已掌握／學習中／需加強；
  進度條加淺色的「學習中」段（`progressBar` 第五參數）；
  學習頁與統計頁直接把規則寫出來；首頁沒有到期項目時改說「明天有 N 項到期」。
  實測修正後：第 1 天就看得到「學習中 20」，第 4 天起已掌握開始往上走。
  audit 增至 **97 項**，新增「入口參數傳遞」6 項與「掌握度會前進」7 項；
  兩條核心斷言拿線上舊版回測**確實是紅的**
  （`buildSession 對任何 src 都只出指定級別` FAIL、
  `smart 排程優先送出到期複習題` 顯示「20 題中有 0 題是到期的」）。
- **v1.12.0** — **學習熱力圖** 與 **🩹 弱點清單**。
  熱力圖取代統計頁原本的 14 天長條圖：長條圖只看得到最近兩週，看不出「有沒有養成習慣」，
  而習慣才是語言學習的關鍵變數。深淺**對照使用者自己設的每日目標**，不是當期最大值——
  用相對最大值上色的話，今天只答 3 題但那是本週最多就會變成最深色，看起來很認真其實沒達標。
  格子範圍會隨資料長度自動伸縮（12～53 週），新使用者不必看一整年的空格。
  **踩到的坑**：週對齊若「先退 N 週再對齊週日」，起點會再往前移最多 6 天，
  結果格子涵蓋不到今天——今天學了卻看不到。正確做法是先把最後一欄對到本週再往前數。
  弱點清單（`/weak`，統計頁與錯題本都有入口）回答錯題本回答不了的問題：
  錯題本只看「有沒有錯過、修好了沒」，是二元的；同樣錯 1 次，練 3 次錯 1 次和練 12 次錯 1 次
  困難度完全不同，而「錯很多次但最後升到已掌握」的項目會被錯題本標成已解決、整個消失，
  但它正是最該回頭複習的。排序用錯誤率 0.55 ＋ 尚未掌握 0.30 ＋ 累計錯誤次數 0.15（10 次封頂），
  作答未滿 3 次的不列入（次數太少看不出是不是真的弱）。可直接「針對這些練一輪」，
  `buildSession` 新增 `src: 'weak'`。
  audit 增至 **84 項**。
- **v1.11.0** — **⏱️ 模擬考計時模式**（`/exam`，首頁與學習頁入口）與 **複習預測日曆**。
  模擬考和「有計時的測驗」的差別在**作答當下不給對錯**：分科計時、可跳題、可回頭改答案，
  全科結束才一次結算——這才是考場的樣子。時間不是隨便訂的，照真實 JLPT 的
  「總時間 ÷ 題數」節奏給（N5 45 秒／題 → N1 65 秒／題），所以練到的是實際作答速度。
  科目對應到 App 真的有的題型：文字・語彙（漢字讀音＋語意）、文法（例句填空＋文法判斷）、
  聽解（裝置有日文語音時才出現）。合格判定照官方基準換算（各科 19/60＝32%，
  總分依級別 80–100/180）。**沒有讀解題庫，所以模擬考不含讀解，UI 直接講明**，
  不假裝分數等同真實考試。成績存在 `meta` store 的 `examHistory`（保留最近 20 次），
  沒有動 IndexedDB schema。**未作答的題目算錯但不寫入 SRS**——時間到來不及寫，
  不代表這個詞學錯了。
  複習預測日曆補在 `/review`：只顯示「下一次複習是什麼時候」看不出負擔會不會塞車，
  SRS 間隔是倍增的，同一天學的東西會在未來同一天一起到期。畫出未來 14 天的到期量，
  才看得到哪天會爆量、可以提前分攤；逾期與 14 天後的另外計數，不混進當日長條。
  audit 增至 **74 項**（新增模擬考 6 項、預測日曆 4 項，含用加速 `setInterval`
  真的跑完計時來驗「時間到自動交卷」）。
- **v1.10.0** — **全級別文法題庫增補**：368 → **606 點**（+238），五級拉齊
  （N5 88→**116**、N4 91→**119**、N3 91→**125**、N2 51→**126**、N1 47→**120**）。
  缺口最大的 N2／N1 各補 75／73 點，依歷屆出題頻率優先，不為湊數收冷僻句型。
  N2 補時間關係（あげく／末に／をきっかけに／を契機に／てはじめて）、判斷根據
  （ことから／からといって／からして／から見ると）、逆接讓步（にもかかわらず／
  にしても／にしろ～にしろ／ものの類）、程度評價（というものだ／ことか／てならない／
  に越したことはない）與書面複合助詞（にわたって／を通じて／のもとで／を除いて）；
  N1 補兼帶動作（かたがた／かたわら／がてら／そばから）、極端程度（の至り／の極み／
  限りだ／この上ない／てやまない）、強烈斷定（べからず／べくもない／んばかり／
  ずには済まない）、書面連接（に即して／に照らして／を踏まえて／を経て／と相まって）
  與接續慣用（ならまだしも／はさておき／と思いきや／ときたら）；
  N5／N4／N3 補的是原本整組缺席的基礎項：N5 的接續詞（でも／それから／だから）、
  助數詞與時間日期表達、疑問詞用法；N4 的活用體系（命令形／禁止形／使役受身形）、
  謙讓尊敬動詞（伺う／存じる／お目にかかる／くださる／おる）；
  N3 的口語縮約（ちゃう／とく／なくちゃ／たって／っけ／かな）與文章接續詞
  （その上／ところで／すると／つまり／ところが／そこで／なぜなら）。
  **新增 audit 斷言「五級份量均衡」**（任一級不足最多者 70% 即失敗）——
  這條若早存在，N2 51／N1 47（不到 N5 的 60%）當初就會被擋下來。
- **v1.9.0** — 新增兩種 JLPT 實戰題型（`js/qtypes.js`，沿用既有測驗 UI／SRS／錯題本，
  入口在學習頁）：
  - **🈯 漢字讀音**（文字語彙第一大題）：題目綁在「詞」上而非單一漢字。
    建了 `漢字寫法→所有合法讀音` 與 `讀音→所有漢字寫法` 兩份索引，
    出「漢字→讀音」時排除**同形異讀**、出「讀音→漢字」時排除**同音異字**
    （全庫有 51 組同音異字，如 雨／飴、風／風邪、暑い／熱い），避免出現兩個正解。
    干擾選項依拍數／首尾音／促音拗音長音一致性評分挑「音近」的，
    片假名與平假名混搭會扣分（否則一眼就被排除）。可出題 2645 詞。
  - **✏️ 例句填空**（文脈規定題型）：`findBlankSpan()` 在例句裡實際定位目標單字或
    文法句型（含活用：食べる→食べ、勉強する→勉強），找不到就不出題；
    以「把正解填回去必須等於原句」驗證挖空位置，空格短於 2 字直接排除。
    干擾選項以**空格前後的助詞是否吻合**為主要依據（後接字相同 +4、前接字相同 +2、
    同詞性 +3、外來語性質一致 +2、長度差、同級別），確保四個選項都塞得進句子。
    **題目刻意不顯示中譯**（顯示了等於給答案），中譯與完整例句在作答後的解說區才出現。
    可出題 2474 條。
  - `buildSession()` 新增 `filter` 參數，讓特殊題型能在組卷階段就篩掉不適用的題目。
- **v1.8.0** — 新增 **🎧 聽力練習**（`/listening`，首頁／學習頁／生活旅行三個入口）。
  10 題一輪，聽日文句子選中文意思，有 ▶播放／🐢慢速／🔁重播；作答後顯示原文＋假名＋中文，
  結果頁列出「需要再聽」清單可逐句重播。作答一樣記入 SRS 與錯題本。
  可出題素材 3517 句（單字例句 2645＋文法例句 606＋旅行 266）。

  **音源決策與降級路徑**（實測四種環境後的設計）：用瀏覽器內建 `speechSynthesis`，
  零成本、免金鑰、本機語音可離線。實測發現**即使裝置上一個語音都沒有，`speak()` 仍會觸發
  `start`／`end` 事件** —— 事件無法證明有出聲，所以：
  - 進入點就用 `hasJapaneseVoice()` 把關，沒有日文語音時**不給死按鈕**，改顯示
    iOS／Android／Windows／Mac 四平台的安裝步驟、「重新偵測」與替代練習入口。
  - 一律由使用者按 ▶ 才發聲（iOS 規定首次 `speak()` 必須在手勢事件中），
    按下時同時用靜音 utterance 解鎖引擎，之後的題目才自動播。
  - 新增 `speakChecked()`：播放時長與文字長度成正比（實測 3 字 1.3 秒、54 字 10 秒），
    低於每字 40ms 判定引擎空轉 → 主動提示「沒有聽到聲音？」並提供「顯示日文原文」的退路。
  - 播兩次還沒作答也給同樣退路；`refreshVoices()` 可重驗（Android 的日文語音常是事後下載的）。
- **v1.7.2** — N2／N1 單字擴充（優化 #3）：N2 260→**502**、N1 263→**514**（+493 條），
  五個級別的單字量終於拉齊（N5 646、N4 501、N3 508、N2 502、N1 514）。
  N2 補和語動詞（抑える／衰える／陥る／貫く／阻む／催す…）、形容詞副詞，
  以及經濟商業、社會政治法律、環境災害、醫療健康、教育研究各領域名詞與 する 動詞；
  N1 補書面語動詞（欺く／操る／戒める／唆す／漂う／凌ぐ／悟る／裁く／弁える…）、
  論理學術詞彙（論拠／仮説／演繹／帰納／蓋然性／整合性）、制度經濟（統治／主権／
  審議／諮問／答申／融資／買収／撤退）、情緒人際（畏敬／憧憬／嫉妬／傲慢／謙虚）、
  文化語言（風潮／様式／語彙／文脈／比喩／婉曲）與 13 個四字熟語。
  新增 1 筆跨級別重複（慌ただしい）已寫進 `data/src/dedup.txt` 交由既有機制處理。
- **v1.7.1** — 文法題庫擴充（優化 #2）：N5 46→**88**、N4 55→**92**、N3 54→**91**（+116 點）。
  補強最薄弱的部分：N5 補基礎助詞（と／や～など／か／に／を／から／までに／ぐらい／だけ／
  しか～ない／ずつ／でも／も）、疑問詞＋か／も、こそあど連體詞、與 20 多個基本句式；
  N4 以**敬語體系**為主（尊敬語 お～になる／れる・られる／いらっしゃる／おっしゃる／なさる／
  召し上がる／ご覧になる，謙讓語 お～する／参る／申す／いたす／拝見する／～ていただく／
  ～させていただきます／～でございます），加上時態階段（～間／～間に／～ところです 三態）
  與複合動詞（～出す／～直す／～合う／～込む）；N3 補逆接對比、原因根據、立場範圍、
  程度情緒、忠告義務、時間連續、列舉強調六類。
  全部加在來源檔末端、未動既有 id；跨級別重複 0 筆新增。
- **v1.7.0** — 四項效能／健壯性優化：
  - **搜尋改用精簡索引**：`build_data.py` 另產 `data/search-index.json`（不含例句三欄，
    佔題庫 57% 體積）。搜尋從「載 13 個題庫檔、gzip 192KB」變成「載 1 個檔、gzip 75KB」，
    結果立刻出現；例句等完整內容改由卡片捲進畫面時用 `findItem()` 惰性補上。
  - **題庫快取自動失效**：manifest 新增 `dataVersion`（全題庫內容 SHA-1）。SW 把它存成快取標記，
    發現題庫換版就整批清掉重抓，不必再靠人工 bump `sw.js` 的 `VERSION`；
    `manifest.json` 也改成 network-first，避免讀到過期的數量／`dupIds`。
  - **修 cache stampede**：`getManifest`／`loadSet`／`loadTravel`／`loadSearchIndex` 改快取
    「進行中的 Promise」。原本並行呼叫會讓同一個檔被重複請求（實測搜尋頁抓了 5 次 manifest.json）。
  - **匯入語意與備份格式**：匯入從「合併」改為「**取代**」並跳出含備份時間、筆數的確認對話框；
    備份格式升到 v3，多帶 `idScheme` 與 `dataVersion`，匯入前先驗格式（`inspectBackup`），
    id 方案不同會直接擋下。
  - **重置後可以重看引導**：`resetAll()` 一併清掉 `seenGuide`（偏好設定如主題／目標／語速保留），
    並在「統計 → 設定」加「重看引導」入口——已有學習進度時也叫得回來。
- **v1.6.2** — 修好一直失效的**羅馬字搜尋**：`romaji` 欄位 2178 筆全是空的，
  但 `search.js` 會比對它、README 也宣告支援 → 羅馬字查詢永遠沒結果。
  `build_data.py` 新增假名→羅馬字（Hepburn，含促音／長音／拗音）自動產生，
  建置時填入該欄位。`taberu`／`gakkou`／`koohii` 等現在都查得到。
- **v1.6.1** — 全面功能檢測後的修正（新增 `scripts/audit.mjs`，45 項回歸）：
  - 修：單級別練習仍會出到 27 筆已標記隱藏的跨級別重複詞（`loadSet` 改回傳已濾除的清單，
    新增內部 `loadSetRaw` 專供 `findItem` 反查舊 id，既有進度仍不受影響）。
  - 修：學習頁掌握度分母用 `count`、首頁／統計頁用 `activeCount`，兩頁數字不一致；
    且舊版本練過現已隱藏的詞仍計入分子 → **掌握度會顯示 102%、進度條溢出**。
    manifest 新增 `dupIds`，三個畫面一律排除。
  - 修：hash 含畸形百分號編碼（如 `#/search?q=%`）時 `decodeURIComponent` 拋 `URIError`
    導致整頁空白；改用 safeDecode，並讓查詢值裡的 `=`／`?` 不再被截斷。
  - 修：最愛頁取消 ★ 後卡片沒有即時消失（`detailCard` 支援 onFav 回呼）。
  - 修：路由錯誤畫面改用 `textContent` 而非 `innerHTML` 插入錯誤訊息。
- **v1.6.0** — 跨級別重複收錄清理：27 筆同形同義、跨相鄰兩級各收一次的詞（如 断る、慣れる、態度、含む、
  矛盾、著しい）改採「載入時去重」——`data/src/dedup.txt` 列出較高級別那筆，`build_data.py` 標為 `dup`，
  出題（含混合模式）、誘答選項、搜尋結果不再重複；但 JSON 條目與 id 保持不變，`findItem()` 照常解析，
  **既有學習進度／錯題／收藏零影響、無需資料遷移**（三個 opus 代理獨立審查一致採此方案）。
  順帶：修掉混合模式下同義兩選項可能都對的誘答 bug；「掌握度」分母改用 `activeCount`；`sw.js` 版本補齊。
- **v1.5.0** — 題庫大幅擴充：N5 269→646、N4 308→501、N3 274→508（+804 條），依舊制 4/3 級
  出題基準與高頻詞表補常見核心詞彙，涵蓋數量詞、時間方位、家具道具、身心健康、職場學校、
  常用動詞與形容詞、副詞接續詞。`check_data.py` 新增「跨級別重複收錄」偵測（列為警告，
  現存 27 筆待後續處理）；`build_data.py` 改以 LF 寫檔。JLPT 題庫合計 1628→2432。
- **v1.4.0** — 無障礙與體驗：加入鍵盤聚焦外框、`prefers-reduced-motion` 支援、`.sr-only` 報讀文字；
  進度條 `role="progressbar"`、閃卡 `role="button"` 可 Tab 聚焦、測驗選項與結果加 `aria-live` 即時播報；
  作答／翻面後焦點自動移到下一步按鈕，測驗與閃卡全程可純鍵盤操作且不會跳題；
  新增**朗讀語速**設定（0.6–1.1×，持久化）、朗讀在切換畫面時自動停止、語音清單延遲就緒時臨用前重試；
  搜尋頁題庫改限並發並行載入（冷快取首屏 2–4s→~1.1s）。
- **v1.3.0** — 效能與品質回歸：各畫面改動態 `import()` 延遲載入（首次載入 JS 96KB→32KB、
  首屏內容 3.6s→1.2s）；Service Worker 不再於安裝時搶抓題庫，改為載入後背景暖機（離線可用性不變）；
  級別色票深淺色文字對比全數達 WCAG AA；返回鍵永不退出 App；每日圖表改以「作答題數」計（原本閃卡會重複計）；
  `srs.js` 對舊/匯入紀錄防呆；新增 `scripts/regress.mjs`（23 項）與 `scripts/verify-full.mjs`（線上完整驗證）。
- **v1.2.1** — 修正「依情境挑選」展開後無法收合（`.chips` 的 `display:flex` 蓋掉 `[hidden]`）。
- **v1.2.0** — 新增與 JLPT 平行的**生活旅行**分類（情境會話 142／日本人這樣說 54／中日漢字大不同 70，共 266 條），
  獨立入口、獨立統計，沿用閃卡／測驗／SRS／錯題本；搜尋與最愛涵蓋旅行內容；`check_data.py` 加入旅行檢查。
- **v1.1.0** — 題庫擴充至 1628 條（五級單字皆 ≥260、文法皆 ≥46）；新增重點複習（★）、跨級搜尋、日文朗讀（Web Speech）、每日目標設定、鍵盤快捷鍵、首次引導；`check_data.py` 品質檢查腳本；IndexedDB 升級至 v2（累加式，不影響舊資料）。
- **v1.0.0** — 首版：閃卡、四選一測驗、SRS、錯題本、統計、PWA。

## 開發驗證清單

> **現行驗證以三套自動化測試為準**：`scripts/audit.mjs`（148 項）、`scripts/verify-full.mjs`（30 項）、
> `scripts/regress.mjs`（23 項），本機與線上版各跑一次。測試紀律見 [`docs/STATUS.md`](docs/STATUS.md)。
> 下面這份是 **v1.2.0 當時**的手動驗證清單，保留作歷史紀錄；其中「每日長條圖」已改為學習熱力圖（v1.12.0）、
> 匯出格式已升到 v3（v1.7.x）、畫面也多了聽力（`/listening`）、模擬考（`/exam`）、弱點清單（`/weak`）。

v1.2.0 時已實測：

- ✅ 首頁載入、題庫索引與統計顯示正確（合計 2698 條與 manifest 一致）
- ✅ 四選一測驗：JLPT 與旅行三分類皆能多方向出題、選項編號、鍵盤 1–4 作答、即時回饋與解說、結果頁與「重做答錯題」
- ✅ 閃卡：JLPT 與旅行（漢字卡／情境卡）翻卡、自評分、鍵盤快捷鍵、翻卡後顯示 ★／🔊、「再練不會的」
- ✅ SRS：JLPT 與旅行項目皆正確更新 box／到期時間（旅行 level 記為 `TRAVEL`）
- ✅ 錯題本：JLPT 與旅行答錯皆自動收錄、篩選（含「生活旅行」）、移除、★／🔊、掌握後自動訂正
- ✅ 重點複習：任意處點 ★ 收藏（含旅行）、`/favorites` 列表可篩「旅行」、可組閃卡／測驗
- ✅ 搜尋：跨全部級別＋生活旅行，漢字差異可搜到並直接收藏
- ✅ 統計：每日長條圖、正確率、連續天數、各級掌握度、**生活旅行掌握度分開顯示**、每日目標滑桿、朗讀開關（皆持久化）
- ✅ `/travel` 入口：三分類卡片與進度、閃卡／測驗、情境會話可依「場景」細分練習、全部混合練習
- ✅ 深／淺色切換並記憶
- ✅ **資料相容性**：既有 IndexedDB 於 v1→v2 升級後資料保留；匯出為 v2、可匯入 v1 或 v2 格式備份；`resetAll` 保留設定
- ✅ 全部 12 條路由（含所有旅行模式）0 console error；全部 JS 通過 `node --check`

> **關於 PWA 離線**：Service Worker 安裝時預先快取 App 外殼（HTML/CSS/JS/manifest/icons），
> 頁面載入後 1.5 秒再於背景把 14 個資料檔（13 個題庫檔＋`search-index.json`）快取起來（`WARM_DATA` 訊息）→ 首次載入快、之後完整離線可用。
> （v1.2.0 當時外殼約 56KB、題庫 13 檔；v1.7.0 起 `manifest.json` 改為 network-first，題庫快取由 `dataVersion` 自動失效。）
> 已用無痕 headless Chrome 實測：斷網後首頁、各級測驗、生活旅行閃卡、統計皆可運作。
>
> **關於朗讀**：Web Speech API 是否有日文語音因裝置而異。iOS／macOS Safari、Windows Chrome 通常內建；
> 未偵測到日文語音時朗讀鈕會隱藏，設定頁也會提示。

---

## 授權 / 內容說明

題庫例句為本專案自行撰寫，供學習用途。JLPT 為日本語能力試驗的名稱，本專案與其主辦單位無關。
